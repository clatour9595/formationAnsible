---
- hosts: all 
  become: true
  task:
    # - name: udapte distribution
    #   apt:
    #     udapte-cache: yes
    - name: install git
      apt:
        name:
          - git
          - nodejs
          - npm
        update_cache: yes